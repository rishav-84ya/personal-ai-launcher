name: Start Personal AI

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  start-ai:
    runs-on: ubuntu-latest

    steps:

      - name: Install Kaggle CLI
        run: |
          python -m pip install --upgrade pip
          python -m pip install --upgrade kaggle
          kaggle --version

      - name: Verify Kaggle authentication
        env:
          KAGGLE_API_TOKEN: ${{ secrets.KAGGLE_API_TOKEN }}
        run: |
          kaggle kernels list --mine --page-size 5

      - name: Download Personal AI notebook
        env:
          KAGGLE_API_TOKEN: ${{ secrets.KAGGLE_API_TOKEN }}
          KAGGLE_KERNEL: ${{ secrets.KAGGLE_KERNEL }}
        run: |
          mkdir -p /tmp/personal-ai

          kaggle kernels pull \
            "$KAGGLE_KERNEL" \
            -p /tmp/personal-ai \
            -m

          echo "Downloaded files:"
          ls -la /tmp/personal-ai

      - name: Start Kaggle notebook with T4 GPU
        env:
          KAGGLE_API_TOKEN: ${{ secrets.KAGGLE_API_TOKEN }}
        run: |
          kaggle kernels push \
            -p /tmp/personal-ai \
            --accelerator NvidiaTeslaT4

      - name: Show completion
        run: |
          echo "Kaggle notebook startup request submitted."
