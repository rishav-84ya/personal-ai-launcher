name: Start Personal AI

on:
  workflow_dispatch:

jobs:
  start-ai:
    runs-on: ubuntu-latest

    steps:
      - name: Install Kaggle CLI
        run: |
          python -m pip install --upgrade kaggle

      - name: Download notebook
        env:
          KAGGLE_API_TOKEN: ${{ secrets.KGAT_d3838476cbdfface3ea088863633266b}}
          KAGGLE_KERNEL: ${{ secrets.tipsofficial/fresh-llm-clude}}
        run: |
          mkdir -p /tmp/personal-ai

          kaggle kernels pull \
            "$KAGGLE_KERNEL" \
            -p /tmp/personal-ai \
            -m

      - name: Start Kaggle notebook
        env:
          KAGGLE_API_TOKEN: ${{ secrets.KGAT_d3838476cbdfface3ea088863633266b}}
        run: |
          kaggle kernels push \
            -p /tmp/personal-ai \
            --accelerator NvidiaTeslaT4
